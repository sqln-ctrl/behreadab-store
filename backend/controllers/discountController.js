import asyncHandler from 'express-async-handler';
import supabase from '../config/supabase.js';
export const getDiscounts    = asyncHandler(async (req, res) => { const {data,error}=await supabase.from('discounts').select('*, products(id,name,image,price)').eq('is_active',true); if(error) return res.status(400).json({message:error.message}); res.json(data); });
export const getAllDiscounts  = asyncHandler(async (req, res) => { const {data,error}=await supabase.from('discounts').select('*, products(id,name,image,price,original_price)').order('created_at',{ascending:false}); if(error) return res.status(400).json({message:error.message}); res.json(data); });
export const createDiscount  = asyncHandler(async (req, res) => { const {product_id,type,value,ends_at}=req.body; if(!product_id||!type||!value) return res.status(400).json({message:'product_id, type and value required'}); const {data:product}=await supabase.from('products').select('price').eq('id',product_id).single(); if(!product) return res.status(404).json({message:'Product not found'}); const disc=type==='percentage'?product.price-(product.price*value/100):product.price-value; await supabase.from('products').update({original_price:product.price,price:Math.max(0,Math.round(disc)),badge:'Sale'}).eq('id',product_id); const {data,error}=await supabase.from('discounts').insert({product_id,type,value,ends_at:ends_at||null}).select().single(); if(error) return res.status(400).json({message:error.message}); res.status(201).json(data); });
export const removeDiscount  = asyncHandler(async (req, res) => { const {data:d}=await supabase.from('discounts').select('*, products(original_price)').eq('id',req.params.id).single(); if(!d) return res.status(404).json({message:'Not found'}); if(d.products?.original_price) await supabase.from('products').update({price:d.products.original_price,original_price:null,badge:null}).eq('id',d.product_id); await supabase.from('discounts').update({is_active:false}).eq('id',req.params.id); res.json({message:'Removed'}); });
